# PrismFlow system plugins

This package-owned directory represents the immutable system-plugin plane. System plugin manifests and executable implementations ship with `@prismflow/dsh`; Dashboard users cannot replace or delete them.

Runtime catalog authority remains the signed/pinned `@prismflow/dsh` package. Do not place user code here.
